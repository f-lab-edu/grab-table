spring:
  datasource:
    url: jdbc:mysql://localhost:3307/grab_table
    username: admin
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      show_sql: true
      format_sql: true
  output:
    ansi:
      enabled: always

